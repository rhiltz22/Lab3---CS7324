# Although our model was quite well optimized the SKlearn implementation still edged it out by a little with about a 0.2% better accuracy than our best model, logistic regression using Newton's method.
# They also provide better numerical stability and the model is more widely tested and well maintained.
